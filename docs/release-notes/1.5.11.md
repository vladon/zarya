# Zarya 1.5.11

## Application services

- Extracted provider selection, config preparation, readiness polling, and
  system-proxy transitions into the runtime coordinator.
- Extracted profile persistence and background worker ownership/cancellation
  from `MainForm`.
- Kept `MainForm` focused on constructing the LCL view and delegating events.
- Added focused regressions for profile services, background lifecycle, and
  the 100 ms / 5 second readiness contract.
