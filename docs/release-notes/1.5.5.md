# Zarya 1.5.5

## Embedded Xray test worker protocol

- Disabled Xray console logging inside isolated test workers so stdout remains a single JSON response.
- Made the GUI parser tolerate diagnostic lines before the final worker response.
- Added unit and real-process regressions for the worker JSON protocol.
