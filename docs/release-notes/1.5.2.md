# Zarya 1.5.2

## Embedded sing-box hard cut

- Rule-set JSON compilation now runs in the embedded sing-box ABI through authenticated `zarya-helper` IPC.
- Removed the external sing-box executable path, CLI rule-set compiler and orphan-process cleanup.
- TUN validation and diagnostics keep configuration in memory.