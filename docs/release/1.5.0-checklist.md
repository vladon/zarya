# Zarya 1.5.0 Release Readiness Checklist

## Automated gates

- [x] Windows static Qt 6.8.3 build and full CTest pass
- [x] Ubuntu build and full CTest pass
- [x] macOS build and full CTest pass
- [x] EN/RU translation check passes
- [x] Windows, Linux, and macOS archives pass manifest/checksum verification
- [x] All three archives pass the user-data and secret audit

Evidence: release-readiness PR #96, GitHub Actions run `30493088225`
(Windows 21m25s, Ubuntu 13m43s, macOS 8m57s).

## KDE apply and restore evidence

- [x] `zarya_linux_proxy_test` covers KConfig 6 and 5 selection, complete
  snapshot, apply, KIO reload, exact restore, idempotency, partial-write
  rollback, reload rollback, invalid input, and missing facilities.
- [x] On 2026-07-29, isolated native `kwriteconfig6`/`kreadconfig6` and
  `kwriteconfig5`/`kreadconfig5` apply/restore smokes passed with a temporary
  `XDG_CONFIG_HOME`; the pre-apply `kioslaverc` files matched byte-for-byte
  after restore.
- [x] On 2026-07-29, the KIO
  `org.kde.KIO.Scheduler.reparseSlaveConfiguration` signal succeeded on an
  isolated session bus.
Interactive Plasma desktop follow-up (not executed for 1.5.0): repeat
start/stop, tray exit, and startup recovery on Plasma 5 or 6 and record the
environment and result below. The 1.5.0 Go decision accepts this residual
interaction risk because the native KConfig/KIO smokes and exhaustive backend
tests above passed.

| Date | Environment | Apply | Stop restore | Exit restore | Recovery | Evidence |
|------|-------------|-------|--------------|--------------|----------|----------|
| — | — | [ ] | [ ] | [ ] | [ ] | — |

## Artifact review

- [x] Archive layouts contain only packaged application/runtime files
- [x] No `profiles.json`, `subscriptions.json`, runtime configs, tokens, private
  keys, credential files, or raw credential-bearing share links
- [x] Release manifests report version `1.5.0`, channel `stable`, and correct
  per-binary checksums
- [x] Published checksum sidecars match the downloaded archives

Post-publication readback on 2026-07-30 downloaded all seven release assets
from GitHub and verified the combined and per-archive SHA-256 files:

- Windows:
  `fa627e753c46aedf77016ee7ba18c3b60d0ef598baab9b8a2600f7185c05715a`
- Linux:
  `de8fa747034631bf94e82c245e48a6e113502086726d51b5d398d70a9bd9028c`
- macOS:
  `55273a19bf1a9957bb948efd3ace547714a5108c590e8ecef0b2cd5bfcd5d50c`

## Decision

- [x] No release blocker from `docs/release/blockers.md`
- [x] Regression matrix completed; hardware-dependent interactive rows that
  were not run remain explicitly unchecked and are accepted follow-ups
- [x] Release notes and GitHub release text reviewed
- [x] Go

Final pre-tag evidence: release PR #97, GitHub Actions run `30495897356`
(Windows 20m21s, Ubuntu 13m44s, macOS 11m47s).

Tagged release evidence: GitHub Actions run `30497172838` passed on Windows,
Linux, and macOS. Tag `v1.5.0` resolves to merge commit
`ce3dfd7e276449cf6eca7a003e3c9ecd911bd48e`. The published Latest release is
<https://github.com/vladon/zarya/releases/tag/v1.5.0>.
