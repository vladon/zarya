# Desktop App Toolkit icon overlays

Missing icons referenced by `lib_ui` styles but not present upstream
(restored from historical `dropdown_submenu_arrow` assets for the Zarya spike).

`codegen_style` searches `-Ithird_party/desktop-app/Resources`.
