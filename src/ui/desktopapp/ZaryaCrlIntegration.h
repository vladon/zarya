#pragma once

// Provides crl::on_main_update_requests() required by lib_ui when
// CRL_ENABLE_RPL_INTEGRATION is set (application-owned hook).

namespace zarya {
// Intentionally empty — symbol lives in namespace crl.
}
