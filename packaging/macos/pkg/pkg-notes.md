# macOS PKG notes (0.31 skeleton)

PKG is reserved for privileged helper / LaunchDaemon installation.

**Not used for 1.0 system-proxy distribution.**

Requires signing, notarization, and careful pre/postinstall scripts.

See `scripts/` placeholders. Do not run destructive install scripts in 0.31.
