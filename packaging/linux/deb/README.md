# Debian package skeleton (0.31)

Placeholder `deb` packaging for future production install.

## Files

- `DEBIAN/control.in` — package metadata template
- `DEBIAN/postinst` — placeholder
- `DEBIAN/prerm` / `postrm` — placeholder

Not built in 0.31. Tarball remains the beta artifact.
