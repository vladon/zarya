Xray is embedded in Zarya and is not installed in this directory.

`cores/xray/` contains mutable geo assets and matcher cache data. The pinned
runtime source is described by `third_party/xray-core.zarya.json` and updated with
`scripts/update-vendored-xray.ps1`.

sing-box remains an external managed executable for experimental TUN.
